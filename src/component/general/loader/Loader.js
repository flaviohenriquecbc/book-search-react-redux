import React from 'react';

const Loader = () => {
    return (
        <div className="loader">
            <div className="color" />
            <div className="color" />
        </div>
    );
};

export default Loader;